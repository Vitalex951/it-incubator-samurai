import React from 'react';
import {LinearProgress} from "@mui/material";

export const Preloader = () => {
    return (
        <div style={{margin: '0'}}>
            <LinearProgress/>
        </div>
    );
};